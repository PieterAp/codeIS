using somiod.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;

namespace somiod.Controllers
{
    //Allowed requests types: GET / POST / PUT / DELETE

    /*
    [RoutePrefix("api/somiod/{application}/")]
    public class ModulesController : ApiController
    {

        [Route("")]
        public IHttpActionResult PostModule(string application, [FromBody] Module module)
        {
            return Ok(module);
        }

        [Route("")]
        public IHttpActionResult GetAllModules(string application, [FromBody] Module module)
        {
            return Ok(module);
        }

        [Route("")]
        public IHttpActionResult GetModule(string application, [FromBody] Module module)
        {
            return Ok(module);
        }
    }
    */
}
